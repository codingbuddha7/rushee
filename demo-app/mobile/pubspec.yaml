name: quick_notes
description: Quick Notes app — Rushee demo.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^9.1.5

flutter:
  uses-material-design: true
