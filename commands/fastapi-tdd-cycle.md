---
name: fastapi-tdd-cycle
description: Implement a Feature Card (FDD) in the FastAPI (Python) backend. Same BDD/TDD pipeline as tdd-cycle; same OpenAPI and Gherkin.
usage: /rushee:fastapi-tdd-cycle <FDD-NNN>
example: /rushee:fastapi-tdd-cycle FDD-001
---

Invoke the **fastapi-tdd-implementer** agent. Prerequisites: Feature Card, OpenAPI spec, Gherkin and step defs (RED). Domain pure; routers thin. Output: backend feature with acceptance and unit tests green.
